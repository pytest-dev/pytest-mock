name: test

on: [push, pull_request]

jobs:

  test:

    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        session:
          - "tests-3.7"
          - "tests-3.8"
          - "tests-3.9"
          - "tests-3.10",
          - "tests-3.11",
          - "tests_norewrite-3.11"
        os: [ubuntu-latest, windows-latest]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
    - name: Install nox
      run: |
        python -m pip install --upgrade pip
        pip install nox
    - name: Test
      run: |
        nox -e ${{ matrix.session }}
